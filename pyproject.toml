[project]
name = "capture-the-segment"
version = "0.1.0"
description = "Scripts to authenticate with Strava and scrape club segment leaderboards."
authors = [
    {name = "FBO"}
]
requires-python = ">=3.11"
dependencies = [
    "selenium>=4.6.0",
    "requests>=2.25.0",
    "beautifulsoup4>=4.9.0",
    "pandas>=1.2.0"
]
